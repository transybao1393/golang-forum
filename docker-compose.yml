version: '3'
services:
  web:
    build: .
    ports:
      - "9000:9000"
    command: bash -c "
      /wait-for-it.sh redis:6379 -- ./nodebb upgrade 
      && ./nodebb setup 
      && ./nodebb start"
  redis:
    image: "redis:alpine"
    restart: on-failure